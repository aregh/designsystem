/** Stjernerangering (★) brukt under kursdeltakar-sitat. */
export interface StarRatingProps {
  /** Tal fylte stjerner, 0–5 (default 5) */
  count?: number;
  /** Glyf-storleik i px */
  size?: number;
  style?: React.CSSProperties;
}
